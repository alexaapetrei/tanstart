import { vi } from "vitest";

export const addPhotoServerFn = vi.fn();
export const deletePhotoServerFn = vi.fn();
export const getPhotosServerFn = vi.fn(); // Though not directly tested by these unit tests, good to have
